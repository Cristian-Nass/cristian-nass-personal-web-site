import transition from '../transition';

const Experiences = () => {
  return (
    <div className="bg-slate-400 bg-cover top-0 left-0 absolute h-full w-full">
      Experiences Page
    </div>
  );
};

export default transition(Experiences);
